overwrite: true
schema: "http://localhost:5400/graphql"
documents: "src/graphql/**/*.ts"
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
hooks:
  afterOneFileWrite:
    - prettier --write
    - eslint --fix
